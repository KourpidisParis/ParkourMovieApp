<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorDarkRed</item>
        <item name="colorPrimaryDark">@color/colorDarkRed</item>
        <item name="colorAccent">@color/colorBlack</item>
    </style>

    <style name="TextAppearance.AppCompat.Widget.ActionBar.Title." parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textSize"> 50dp</item>
        <item name="android:textColor" >@color/colorDarkRed</item>
        <item name="android:fontFamily">monospace</item>
    </style>

</resources>
